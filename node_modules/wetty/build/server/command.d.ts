import type { SSH } from '../shared/interfaces';
import type { Socket } from 'socket.io';
export declare function getCommand({ request: { headers }, client: { conn: { remoteAddress }, }, }: Socket, { user, host, port, auth, pass, key, knownHosts, config, allowRemoteHosts, }: SSH, command: string, forcessh: boolean): [string[], boolean];
