# Alpha Vantage API Key (for stock market data)
# Get your free API key at: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=your_api_key_here

# Sonarr Configuration
# Base URL for your Sonarr instance
SONARR_URL=http://localhost:8989
# Sonarr API key (found in Settings -> General -> Security -> API Key)
SONARR_API_KEY=your_sonarr_api_key_here

# Vectorization Configuration for Article Clustering
# Method: 'embeddings' (default, uses Azure OpenAI) or 'tfidf' (classical approach)
VECTORIZATION_METHOD=embeddings

# TF-IDF Settings (only used if VECTORIZATION_METHOD=tfidf)
# Maximum number of features (vocabulary size)
TFIDF_MAX_FEATURES=5000
# Minimum document frequency (ignore terms appearing in fewer documents)
TFIDF_MIN_DF=1
# Maximum document frequency (ignore terms appearing in more than this fraction of documents)
TFIDF_MAX_DF=0.85
# N-gram range (1,1 for unigrams only, 1,2 for unigrams+bigrams)
TFIDF_NGRAM_RANGE=1,2
# Similarity threshold for clustering (TF-IDF typically needs lower threshold ~0.3)
TFIDF_SIMILARITY_THRESHOLD=0.3

# Embedding Settings (only used if VECTORIZATION_METHOD=embeddings)
# Similarity threshold for clustering (embeddings typically use higher threshold ~0.85)
EMBEDDING_SIMILARITY_THRESHOLD=0.85

# Add other environment variables below as needed
